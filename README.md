# كيف أسأل؟

تطبيق ويب مبني باستخدام Next.js وTailwind CSS لمساعدة المتحدثين بالعربية على صياغة أسئلة واضحة ومؤثرة في مواقف متنوعة.

## ⚙️ المتطلبات

- Node.js 18 أو أحدث
- npm 9 أو أحدث

## 🚀 بدء التشغيل محليًا

```bash
npm install
npm run dev
```

ثم افتح `http://localhost:3000`.

## 🧰 ما الذي ستحصل عليه؟

- ورشة تفاعلية لتجميع عناصر السؤال خطوة بخطوة
- بطاقات سيناريو جاهزة لمواقف مهنية وشخصية شائعة
- قائمة تحقق ونصائح سريعة لصياغة أسئلة فعالة
- تجربة مستخدم مهيأة بالكامل للاتجاه من اليمين لليسار

## 📦 الأوامر المتاحة

- `npm run dev` لتشغيل بيئة التطوير
- `npm run build` لإنشاء نسخة الإنتاج
- `npm run start` لتشغيل نسخة الإنتاج محليًا
- `npm run lint` لتشغيل الفاحصات

## 🗂️ بنية الملفات

```
app/
  components/         مكونات الواجهة القابلة لإعادة الاستخدام
  sections/           أقسام الصفحة الرئيسية
  globals.css         أنماط Tailwind العامة
``` 

## 🌐 النشر

تم تجهيز المشروع للنشر على Vercel. بعد نجاح `npm run build` يمكن تنفيذ:

```bash
vercel deploy --prod --yes --token $VERCEL_TOKEN --name agentic-07508bfe
```

## 📄 الترخيص

الكود متاح بموجب رخصة MIT.
